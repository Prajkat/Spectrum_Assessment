<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

     <data>
          <variable
              name="movieInformation"
              type="com.example.assesmentapplication.model.response.nowplaying.Result" />
      </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="290dp"
        android:orientation="vertical">

        <androidx.appcompat.widget.AppCompatImageView
            android:id="@+id/item_poster_post"
            android:layout_width="match_parent"
            android:layout_height="240dp"
            android:scaleType="fitXY"
            android:src="@drawable/ic_launcher_background"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:imageUrl="@{`https://image.tmdb.org/t/p/original`+ movieInformation.poster_path}"
            tools:src="@drawable/ic_launcher_background" />

        <TextView
            android:id="@+id/item_poster_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/teal_700"
            android:gravity="center"
            android:padding="5dp"
            android:textColor="@color/white"
            android:textSize="14sp"
            android:textStyle="bold"
            android:text="@{movieInformation.title}"
            app:layout_constraintTop_toBottomOf="@+id/item_poster_post"
            tools:text="title" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
